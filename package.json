{
  "name": "postgres-functions-triggers",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx reveal-md slides.md --static _site --static-dirs=assets",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "uberardy",
  "license": "ISC",
  "dependencies": {
    "reveal-md": "^5.3.2"
  }
}
